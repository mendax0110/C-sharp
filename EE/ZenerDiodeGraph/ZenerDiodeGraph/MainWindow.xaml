<Window x:Class="ZenerDiodeGraphWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:oxy="http://oxyplot.org/wpf"
        Title="Zener Diode Graph" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Margin="5">Voltage:</Label>
        <TextBox x:Name="InputTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>
        <Button x:Name="CalculateButton" Grid.Row="1" Grid.Column="0" Margin="5" Click="CalculateButton_Click">Calculate</Button>
        <Label x:Name="ResultLabel" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <oxy:PlotView Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Model="{Binding ZenerDiodeModel}" />
    </Grid>
</Window>
